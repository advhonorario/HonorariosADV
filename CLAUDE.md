# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status do projeto

`especificacao-sistema-honorarios.md` é a fonte de verdade completa (modelo de dados SQL, RLS, RPCs, telas, identidade visual, metas de performance). Antes de implementar qualquer parte do sistema, leia a seção relevante desse documento — não reimplemente decisões já tomadas nele nem invente estrutura alternativa sem justificativa.

**Fase 1 (banco), Fase 2 (shell, login, cadastros), Fase 3 (lançamento), Fase 4 (edição, estorno, motivo, logs) e Fase 5 (painel com filtros e cruzamento) concluídas.** Próxima fase pendente: Fase 6 (exportações Excel e PowerPoint) — ver especificação §13.

- O painel (`public/js/telas/painel.js`) reconcilia os indicadores do `Contabilidade 2025.pptx` (especificação §2, item pendente P1 — o arquivo chegou em 2026-09-16, fica em `Modelo/` local, **nunca commitado**: tem nomes reais de advogados/clientes e valores de honorários, repositório é público). Estilo dos gráficos segue o padrão desses slides, não o "não-pizza" original da especificação: legenda no topo, sem escala numérica no eixo (valor já vai no rótulo da barra/ponto), pizza/donut permitida para os indicadores explicitamente pedidos assim (divisão por indicador, por faixa de valor). Barras verticais mostram o valor por fora, em `--tinta-900`; barras horizontais por fora à direita, em `--tinta-900`; barra empilhada (indicação × própria) é exceção pedida pelo usuário — valor por dentro, branco.
- `rpc_painel` restringe o perfil `advogado` a lançamentos onde ele aparece no rateio, com valores cheios do lançamento (decisão de produto 2026-09-16) — bug de RLS ausente desde a Fase 1, corrigido na migration `014`. Usa o padrão NULL-safe `v_eh_advogado`/`v_advogado_escopo` (nunca `v_advogado_escopo is null` sozinho como "sem restrição").
- Não existe seletor de múltipla escolha reutilizável até a Fase 5 — `public/js/componentes/multi-select.js` (novo) empacota `combobox.js` + chips removíveis, usado pelos filtros de advogado/cliente do painel.
- A tela de lançamento (`public/js/telas/lancamento.js`) cobre criação, edição em linha, estorno e duplicação de lançamentos; clique numa linha da tabela de recentes abre em modo edição, botão "Estornar" chama `rpc_estornar_lancamento`, ambos exigem motivo via `public/js/componentes/modal-motivo.js` (categoria `lancamento`). Duplo-clique em célula (do mockup §7.5) não foi implementado — o clique-para-editar já cobre correções de valor/percentual sem o risco de conflito de eventos entre clique simples e duplo-clique na mesma linha.
- Não existe tela de cadastro de processos (não está na especificação de telas, §7). O campo "Processo" no lançamento é um combobox sobre a tabela `processos` já existente; para cliente sem processo cadastrado, a tela de Lançar tem um mini-formulário inline ("+ novo processo", só número CNJ opcional) em vez de uma tela dedicada.
- `public/js/telas/logs.js` (Registro de alterações, §7.7) busca direto em `logs_auditoria` com filtros de período/tela/entidade/ação e busca por motivo (a busca não cobre o conteúdo de `dados_antes`/`dados_depois`, só o texto do motivo — buscar dentro do jsonb exigiria uma RPC dedicada, fora do escopo da Fase 4). `registro_ref` não é populado pela trigger `fn_auditoria`; a referência legível de cada linha é resolvida no client (`referenciaLegivel` em `logs.js`) a partir de `dados_depois`/`dados_antes` + cache do `store`. Exportação do log em Excel é Fase 6, não implementada.
- Banco: 19 migrations em `supabase/migrations/` (`001` a `008` da Fase 1; `009_rpcs_cadastros.sql`, `010_realtime.sql` e `011_rpcs_reativar.sql` da Fase 2; `012_lancamento_fase3.sql` e `013_fix_rateio_update_where.sql` da Fase 3; `014` a `019` da Fase 5 — escopo por advogado, indicadores do pptx, fix de window function em aggregate, retenção plurianual, ticket médio segmentado), aplicadas no projeto Supabase real. A Fase 4 não precisou de nenhuma migration nova. Não há ambiente local com Docker configurado nesta máquina — aplicar mudanças com `supabase db push --db-url <POSTGRES_URL_NON_POOLING> --include-all --yes` (variáveis em `.env.local`, nunca commitado) e sempre rodar `supabase db advisors --db-url <...> --type all --level warn` depois, esperando "No issues found".
- **Postgres não deixa usar função de janela (`sum(x) over (...)`) direto dentro do argumento de uma função de agregação (`jsonb_agg(...)`) na mesma consulta** — erro "aggregate function calls cannot contain window function calls". Bug real cometido e corrigido na Fase 5 (migration `016`): materializar a window function numa subconsulta antes de agregar.
- **O Supabase de produção roda com a extensão `safeupdate` ativa.** Todo `UPDATE`/`DELETE` — mesmo dentro de função `security definer`, mesmo em tabela temporária — precisa de `WHERE` explícito, senão falha em runtime com "UPDATE requires a WHERE clause" (vira HTTP 400 no PostgREST). Use `where true` quando a atualização é intencionalmente para todas as linhas. Bug real encontrado e corrigido na Fase 3 (`013_fix_rateio_update_where.sql`) — o algoritmo de maior resto de `rpc_salvar_lancamento` (escrito na Fase 1) tinha um `UPDATE` sem `WHERE` que nunca tinha sido exercitado até a tela de lançamento existir de verdade.
- Frontend: `public/` (HTML/CSS/JS modular, sem build). Rodar localmente com `npx serve public`. Deploy: projeto Vercel `adv-honorarios/honorarios-adv`, já conectado ao repo GitHub (deploy automático a cada push em `main`) — produção em `https://honorarios-adv.vercel.app`. `SUPABASE_URL`/`SUPABASE_ANON_KEY` ficam hardcoded em `public/js/supabase.js` (são públicas por design, não há build step para injetar env vars no client).
- Usuário admin inicial já existe no Supabase (criado manualmente via SQL, não pelo fluxo de signup — não há tela de cadastro de usuário nesta fase).
- Extensão do Chrome (`claude-in-chrome`) não conectou nas sessões usadas até agora — mudanças de UI foram verificadas por chamadas REST diretas à API do Supabase e por testes manuais do usuário no ambiente de produção, não por automação de navegador.

## Stack e arquitetura

- **Frontend**: HTML + CSS + JS modular (ES Modules nativos), sem framework e sem etapa de build — `supabase-js` v2 via CDN. Isso é uma decisão deliberada (especificação §3), não uma limitação temporária; só migrar para Next.js se surgir necessidade de SSR, portal do cliente ou área pública indexável.
- **Backend**: Vercel Serverless Functions (Node 20) em `/api/*`, usadas apenas para as operações que exigem bibliotecas Node (exportação `.xlsx` via ExcelJS, `.pptx` via PptxGenJS) — nunca para lógica de negócio que pode viver em RPC do Postgres.
- **Dados**: Supabase (Postgres + Auth + RLS + Storage). Toda escrita de negócio passa por RPC `security definer`; o client nunca grava direto nas tabelas.
- **Ambientes**: branch `main` → Supabase `honorarios-prod`; branch `develop` → Supabase `honorarios-dev`. Preview deploys da Vercel apontam para o Supabase de homologação.
- Estrutura de diretórios planejada (especificação §12): `public/js/telas/*` por tela, `public/js/componentes/*` para UI compartilhada, `api/*` para as três functions, `supabase/migrations/*` numeradas sequencialmente.

## Regras de domínio não-óbvias

Estas regras vêm de decisões explícitas na especificação e são fáceis de violar sem reler o documento inteiro:

- **`valor_bruto` × `valor_base`**: bruto é o que o cliente pagou; base é o que entra no rateio (bruto menos custas/taxas/repasses). Por padrão são iguais. Nunca confundir os dois em cálculos.
- **Arredondamento por maior resto** (especificação §4.8): cada rateio é `round(base × percentual/100, 2)`; o resíduo de centavos vai para quem tem a maior parte fracionária descartada; o residual do escritório é sempre `valor_base − Σ valor_i` por diferença, nunca por percentual. O mesmo algoritmo deve rodar idêntico no navegador (preview) e no Postgres (gravação); o servidor é sempre a fonte da verdade em caso de divergência.
- **Lançamento nunca é apagado.** "Excluir" na UI sempre chama estorno (`status = 'estornado'`), preservando o registro no histórico. Exclusão física só existe para rascunhos nunca confirmados.
- **Toda alteração em registro já salvo, estorno, inativação ou remoção de linha de rateio exige motivo** (mínimo 10 caracteres), gravado via `set_config('app.motivo', ...)` antes da escrita, capturado pela trigger genérica de auditoria (`fn_auditoria`) em `logs_auditoria`. Não implementar nenhum caminho de escrita que contorne essa trigger.
- **`set_config` + o `update` precisam estar na mesma chamada.** O Supabase Data API (PostgREST) executa cada `update()`/`insert()` feito via REST como sua própria transação isolada — não dá para chamar `set_config` numa requisição e um `update` direto na tabela em outra e esperar que o motivo seja capturado. Por isso toda ação que exige motivo (estorno, inativação, alteração de percentual padrão, remoção de indicação) é uma RPC `security definer` que faz `set_config` + a escrita dentro do mesmo `plpgsql`. Edições sem motivo continuam podendo ser `update`/`insert` direto via REST, protegidas só por RLS.
- **RPCs `security definer` ignoram a RLS.** Toda RPC de escrita precisa checar o perfil explicitamente logo no início, e a checagem tem que ser NULL-safe: `coalesce(private.perfil_atual()::text, '') not in ('admin','financeiro')`, nunca `private.perfil_atual() not in (...)` puro — com perfil `NULL` (usuário autenticado sem linha em `usuarios`), a versão sem `coalesce` falha **aberta** (não dispara a exceção) em vez de negar acesso. Bug já cometido e corrigido nas RPCs da Fase 1; não reintroduzir nas próximas.
- **Realtime do Supabase não funciona "de graça".** `postgres_changes` só entrega eventos para tabelas explicitamente adicionadas à publicação `supabase_realtime` (`alter publication supabase_realtime add table ...`) — criar a tabela e assinar o canal no client não é suficiente. Faltou isso para `advogados`/`tipos_processo`/`tipos_servico` até a migration `010_realtime.sql`; qualquer tabela nova que `store.js` (ou equivalente futuro) precise observar via `postgres_changes` tem que entrar nessa publicação também, senão a lista só atualiza com F5 manual.
- **Nunca depender só do Realtime para refletir a própria escrita.** Além de habilitar a publicação, `public/js/store.js` expõe `recarregarAdvogado`/`recarregarTiposProcesso`/`recarregarTiposServico` para as telas chamarem explicitamente logo após um `insert`/`update`/RPC bem-sucedido — dá feedback imediato e não depende de latência/config do Realtime. Ao criar uma tela nova que escreve em algo cacheado no `store`, seguir esse padrão (escrever → recarregar explicitamente → deixar o `store.on(...)` já existente re-renderizar).
- **Resolução de percentual sugerido** segue ordem estrita do mais específico ao mais genérico (advogado+processo+serviço → advogado+serviço → advogado+processo → percentual padrão do advogado → percentual sugerido do serviço → zero). A origem escolhida é sempre gravada em `origem_percentual`.
- **RLS por perfil** (`admin`, `financeiro`, `socio`, `advogado`, `leitura`): perfil `advogado` só enxerga seus próprios números; `service_role` só é usada dentro de Serverless Functions, nunca em código que chega ao client.
- Textos de interface e nomes de campo são em pt-BR, por extenso, sem abreviação — segue o padrão já usado no SQL da especificação (`percentual_padrao`, não `pct_pad`).

## Segredos

Nunca versionar a pasta `Senhas/` nem arquivos `.env*` (já cobertos pelo `.gitignore`). O repositório GitHub (`advhonorario/HonorariosADV`) é **público** — qualquer coisa commitada e enviada (`git push`) fica visível e persiste no histórico mesmo após remoção posterior.
